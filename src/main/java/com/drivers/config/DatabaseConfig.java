package com.drivers.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // на данный момент данный класс не нужен,
    // так как подключение к бд, запись created_at/updated_at
    // работает автоматически через аннотации спринга
}
